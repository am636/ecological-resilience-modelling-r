# Data

The example workflow generates synthetic input data when it is run.

Use this folder for local input data if adapting the workflow to a real dataset. Raw or sensitive data should not be committed to the repository.
